Readme

1) Open the FullSystem.slx and MatlabSimulation.mlx.
2) Run the MATLAB file so that the required variables are stored in the workspace.
3) Run the FullSystem.slx file and use the scope to see the response. 